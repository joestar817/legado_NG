<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/Ng.ProviderCard"
    android:gravity="center_vertical"
    android:orientation="horizontal">

    <FrameLayout
        android:id="@+id/layout_select_provider"
        android:layout_width="58dp"
        android:layout_height="match_parent"
        android:clickable="true"
        android:focusable="true"
        android:foreground="?android:attr/selectableItemBackground">

        <View
            android:id="@+id/view_active_indicator"
            android:layout_width="5dp"
            android:layout_height="match_parent"
            android:layout_gravity="start"
            android:background="@drawable/ng_bg_provider_active_indicator"
            android:visibility="gone" />

        <FrameLayout
            android:layout_width="38dp"
            android:layout_height="38dp"
            android:layout_gravity="center"
            android:background="@drawable/ng_bg_icon_circle"
            android:padding="7dp">

            <ImageView
                android:id="@+id/image_icon"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:contentDescription="@string/ai_provider_menu"
                android:scaleType="fitCenter" />

        </FrameLayout>

    </FrameLayout>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="4dp"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/text_name"
            style="@style/Ng.CardTitle"
            android:ellipsize="end"
            android:singleLine="true" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/text_enabled"
                style="@style/Ng.Tag.Success" />

            <TextView
                android:id="@+id/text_model_count"
                style="@style/Ng.Tag.Info"
                android:layout_marginStart="8dp"
                android:minWidth="64dp" />

        </LinearLayout>

    </LinearLayout>

    <ImageView
        android:id="@+id/image_drag_handle"
        android:layout_width="36dp"
        android:layout_height="match_parent"
        android:contentDescription="@string/ai_provider_drag_sort"
        android:paddingStart="8dp"
        android:paddingTop="20dp"
        android:paddingEnd="4dp"
        android:paddingBottom="20dp"
        android:src="@drawable/ic_drag_handle"
        android:tint="@color/ng_on_surface" />

</LinearLayout>
