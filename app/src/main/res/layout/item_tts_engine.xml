<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/Ng.ProviderCard"
    android:gravity="center_vertical"
    android:orientation="horizontal">

    <View
        android:id="@+id/view_selection_indicator"
        android:layout_width="6dp"
        android:layout_height="match_parent"
        android:background="@drawable/ng_bg_tts_engine_selected_indicator"
        android:visibility="gone" />

    <FrameLayout
        android:id="@+id/layout_select_engine"
        android:layout_width="58dp"
        android:layout_height="match_parent"
        android:clickable="true"
        android:focusable="true"
        android:foreground="?android:attr/selectableItemBackground">

        <FrameLayout
            android:layout_width="38dp"
            android:layout_height="38dp"
            android:layout_gravity="center"
            android:background="@drawable/ng_bg_icon_circle"
            android:padding="7dp">

            <ImageView
                android:id="@+id/image_icon"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:contentDescription="@string/speak_engine"
                android:scaleType="fitCenter"
                android:src="@drawable/ic_ai_capability_tts" />

        </FrameLayout>

    </FrameLayout>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="4dp"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/text_name"
            style="@style/Ng.CardTitle"
            android:ellipsize="end"
            android:singleLine="true" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/text_enabled"
                style="@style/Ng.Tag.Success" />

            <TextView
                android:id="@+id/text_type"
                style="@style/Ng.Tag.Info"
                android:layout_marginStart="8dp" />

            <TextView
                android:id="@+id/text_voice_count"
                style="@style/Ng.Tag.Info"
                android:layout_marginStart="8dp"
                android:minWidth="64dp" />

        </LinearLayout>

    </LinearLayout>

    <ImageView
        android:id="@+id/image_drag_handle"
        android:layout_width="36dp"
        android:layout_height="match_parent"
        android:contentDescription="@string/ai_provider_drag_sort"
        android:paddingStart="8dp"
        android:paddingTop="20dp"
        android:paddingEnd="4dp"
        android:paddingBottom="20dp"
        android:src="@drawable/ic_drag_handle"
        android:tint="@color/ng_on_surface" />

</LinearLayout>
